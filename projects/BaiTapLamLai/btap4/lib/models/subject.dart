/// trong này là thông tin của một đối tượng sau khi được lấy từ API về
class Subject {
  final String name;
  final String title;
  // final String description;
  // final String image;

  Subject({
    required this.name,
    required this.title,
    // required this.description,
    // required this.image,
  });
}
